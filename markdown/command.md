## json-server

```
npm install json-server -g
json-server --watch db.json --port 3003
alias jsw="json-server --watch"
source ~/.bash_profile
```

## 更新全局 npm 包

```
npm-check -u -g
npm update -g
npm outdated -g
```