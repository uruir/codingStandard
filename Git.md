## 远程

```
# 查看远程分支名和注释
git branch -av
# 查看远程分支（不全）
git remote -v
# 删除远程分支
git push origin --delete <branchName>
# 删除远程标签
git push origin --delete tag <tagName>

```

## 本地

```
# 重命名本地分支
git branch -m before after
```